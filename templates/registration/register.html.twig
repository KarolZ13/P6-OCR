{% extends 'base.html.twig' %}

{% block title %}Inscription | SnowTricks{% endblock %}

{% block body %}
    {% for flash_error in app.flashes('verify_email_error') %}
        <div class="alert alert-danger" role="alert">{{ flash_error }}</div>
    {% endfor %}
<div style="display: flex; justify-content: center; align-items: center; min-height: 100vh">
    <div class="wrap py-4 bg-white overflow-hidden col-12 col-md-8 col-lg-6 col-xl-4">
        <div class="inscription">
            <h1 class="h3 mb-3 font-weight-normal">Inscription</h1>
        </div>
        {{ form_errors(registrationForm) }}

        {{ form_start(registrationForm) }}
            <fieldset>
            <div class="error-red">
                {{ form_row(registrationForm.username) }}
            </div>
            <div class="error-red">
                {{ form_row(registrationForm.email) }}
            </div>
            <div class="error-red">
                {{ form_row(registrationForm.plainPassword, {
                    label: 'Mot de passe'
                }) }}
                {{ form_row(registrationForm.agreeTerms) }}
            </div>
            </fieldset>
            <div class="inscription mt-3">
                <button type="submit" class="btn btn-secondary my-3">Inscription</button>
            </div>
            <div class="text-center mt-3">
                <a href="{{ path('app_login')}}">Vous avez déjà un compte? Cliquez ici</a>
            </div>
        {{ form_end(registrationForm) }}
    </div>
</div>

{% endblock %}
